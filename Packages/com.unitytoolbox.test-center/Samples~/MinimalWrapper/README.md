# Minimal Wrapper

This sample shows the only project-side code the package expects:

1. define your suites and UI sections
2. create one shared `ReusableTestCenterRunner`
3. expose a window derived from `ReusableTestCenterWindowBase`

Replace the sample categories with categories that exist in your project tests.
